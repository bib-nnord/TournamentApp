docker-compose up

cd apps/frontend
pnpm dev




Critical

Move secrets to .env files (remove hardcoded DB/JWT secrets from docker-compose.yml)
Add test infrastructure (install and configure Jest/Vitest/Playwright; add test files)
Remove hardcoded API URLs (use env/config for API endpoints)
High

Fix notification promises: notifyUser/notifyUsers should be awaited or errors surfaced
Fix clearAuth: ensure user state is cleared on logout/token refresh failure
Add error boundary pages (custom error.tsx for all routes)
Use Next.js router for auth failures (avoid window.location.href)
Add type safety to formatTournament and bracket code (remove any)
Handle null tournament creator references throughout backend
Change Account Deletion to simply anonymizing user rather than deleting
Medium

Implement refresh token rotation (invalidate old tokens)
Add DB indexes to FK columns (e.g., Message.sender_id, Tournament.status)
Add cascade rules for user/team/tournament deletions
Fix race condition on token refresh (prevent multiple concurrent refreshes)
Mark req.user as optional in express.d.ts and handle nulls everywhere
Add a real build step for Docker (multi-stage, compiled JS output)
Remove unused/placeholder /api-docs endpoint
Handle inconsistent req.user null checks in backend
Add ARIA attributes to modals for accessibility
Avoid silent error swallowing (handle all errors explicitly)
Prevent message sender impersonation (add type/role to Message model)
Remove duplicate API_URL definitions (import from single source)
Validate route parameters in frontend pages
Use a request validation library (e.g., zod) in backend controllers
Strengthen frontend validation (registration, forms)
Fix useDebouncedSearch dependency issue
Switch to cursor-based pagination for large datasets
Add schema validation for JSON bracket data


Low

Add generic API error handling in frontend (centralize error parsing)
Check token expiry in useRequireAuth
Optimize Dockerfile (avoid double-copying prisma/)
Improve notification toasts (more info, add UNDO button)
Add error handling for guest registration emails
Add light/dark mode support
Move declined tournaments to bottom and gray them out
Add icon for tournament view

Redesign (general)
Profile improvements
Settings page improvements
PDF export for WIP tournaments
Tailwind/Shadcn/AI Library for Assistant
Full graphical rework
AI Assistant
Swagger Autogen (part 2)
ForgotPasswordForm: implement full functionality
CI/CD setup
Testing (add actual tests)
Notification Toasts: add more info, UNDO button
Email to guest users to create account


other things to manually add:

Tournament display fix date and disciplines. 

Dashboard everything.

friend settings 

messages multi options when selecting many at once as dropdown rather than appearing when relevant also add sorting maybe

profile and setings verything

add some filler text to homepage

Dashboard cards need a limit

More little cards? "you have 5 friend requests, 3 team invitations and 2 tournament invitations"?

