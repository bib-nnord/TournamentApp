Messaging Feature — Remaining TODO
====================================

[x] Phase 1: Core message backend + frontend wiring
[x] Phase 2: Tournament notifications (create, match result, status change)
[x] Participant confirmation (unconfirmed badge, accept/decline from tournament page + messages)

[ ] Run prisma migrate dev (requires DB running) — adds "confirmed" to tournament_participant, "reference_id" to message

[ ] Phase 4: Friend System
    - New Friendship model (requester_id, recipient_id, status: pending/accepted/declined/blocked)
    - Endpoints: POST /friends/request, PATCH /friends/:id/accept, GET /friends, DELETE /friends/:id
    - Friend requests auto-generate a message in "users" category
    - Friends page already exists with mock data — wire to real API

[ ] Phase 5: User-to-User Messages
    - POST /messages endpoint (send message to another user)
    - Add "allow_messages_from" column to User model (everyone / friends_only)
    - Gate message sending based on that setting
    - Frontend: compose/reply UI on messages page
